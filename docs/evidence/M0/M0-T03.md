# M0-T03 - Backend Skeleton Acceptance

## Identity

- Milestone: M0 (trace aliases M02/M05)
- Requirements: API-001, API-003, API-006, API-007, NFR-007, NFR-009, AI-004
- Acceptance criteria: AC-026-AC-028, AC-030, AC-035, AC-037, AC-040, AC-046
- Reviewer: Integration/root
- Date: 2026-08-02

## Delivery

The backend now has a typed FastAPI application factory, versioned router,
secure settings validation, request context, structured redacted logging,
standard success/error envelopes, and injected liveness/readiness probes. It
contains no feature persistence, credentials, database schema, or AI runtime.

## Validation

| Check | Result |
|---|---|
| Ruff all-rules and formatting | Pass; 27 files |
| Strict Mypy with Pydantic plugin | Pass; 27 files |
| Pytest unit/integration/boundary suite | Pass; 26 tests |
| Branch coverage | Pass; 94.22% (threshold 85%) |
| Bandit application scan | Pass; zero findings and zero skipped issues |
| In-process OpenAPI smoke | Pass; `health_live` and `health_ready` at `/api/v1/health/*` |
| Canonical `backend-check` | Pass after Integration added the explicit backend Mypy config |
| Repository policy and `git diff --check` | Pass |

## Contract freeze F2

F2 freezes `create_app`, `/api/v1`, operation IDs `health_live` and
`health_ready`, `{data,meta}` success envelopes, the documented error envelope,
request-ID propagation, and dependency-injected readiness. OpenAPI export and
generated-client ownership remain M0-T06; T05 replaces the unavailable default
readiness probe with the database probe.

## Decision

M0-T03 passes. M0-T05 may begin with one reserved Alembic head and sole
ownership of `backend/app/infrastructure/database/**`.
