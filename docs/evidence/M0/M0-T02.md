# M0-T02 - Repository and Command Foundation

## Identity

- Milestone: M0 (trace aliases M01/M02)
- Requirements: DOC-003, OSS-001, OSS-002, CHG-001, CHG-002
- Acceptance criteria: AC-043, AC-044, AC-045
- Reviewer: Integration/root
- Date: 2026-08-02

## Delivery

The repository now has governance files, issue and pull-request templates,
developer documentation indexes, a cross-platform Python task runner, Makefile
forwarders, and pinned local pre-commit hooks. Application-specific commands
fail explicitly until their workspaces are dispatched.

## Validation

| Check | Result |
|---|---|
| Required governance/repository files | Pass; 15/15 present or explicitly blocked (`LICENSE`) |
| Markdown relative-link validation | Pass |
| JSON/TOML/UTF-8/final-newline checks | Pass |
| Machine-specific path and likely secret/private-key scan | Pass |
| Issue-form YAML parse | Pass |
| Task runner help and repository check | Pass |
| Pre-commit configuration validation and local hooks | Pass |
| `git diff --check` | Pass |

## Decision

M0-T02 passes for the repository and command foundation. F1 freezes the public
task names in `scripts/task.py`. `R-018` remains a release blocker: no LICENSE
is created until the maintainer chooses an OSI-compatible license.
