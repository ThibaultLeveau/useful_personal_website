# Contact inbox

Open **Administration → Contacts** to review private form submissions. The default view shows unread messages newest first. Opening a message does not silently change it: use **Mark read**, then **Archive** when handled. An archived message can be restored to read.

Permanent deletion requires explicit confirmation and cannot be undone. The audit trail records only the opaque identifier and prior state, never message content or the sender’s personal details.
