# M0-T05 - Database and Migration Acceptance

## Identity

- Milestone: M0 (trace alias M03)
- Requirements: NFR-006, NFR-007, NFR-011
- Acceptance criteria: AC-034-AC-036
- Reviewer: Integration/root
- Date: 2026-08-02

## Delivery

The backend now has PostgreSQL-only async engine/session construction, a
transaction-owning unit of work, safe readiness and revision probes, and one
deterministic Alembic baseline. The foundation migration intentionally creates
no feature table; only Alembic's revision table exists.

## Validation

| Check | Result |
|---|---|
| Sole Alembic head | Pass; `20260802_0001` |
| Empty PostgreSQL 17.10 upgrade/current/check/downgrade lifecycle | Pass |
| Schema inspection | Pass; only `alembic_version` at the exact head |
| Rollback-on-error and repository/UoW transaction tests | Pass |
| Database readiness and runtime-revision mismatch tests | Pass |
| Full database-backed Pytest | Pass; 38 tests, 93.65% branch coverage |
| Ruff/format/strict Mypy | Pass; 45 files |
| Bandit | Pass; zero findings/skips |
| Isolated validation-container cleanup | Pass; exact task container removed; unrelated containers untouched |

## Contract freeze F3

F3 freezes `APP_DATABASE_URL`, async request-scoped sessions, application-owned
transaction boundaries, a single Alembic head, startup revision checking, and
the explicit migration command `python -m uv run --frozen alembic -c
backend/alembic.ini upgrade head`. Startup never migrates, bootstraps, or seeds.
Full database tests receive a unique empty PostgreSQL URL through
`TEST_DATABASE_URL`.

## Decision

M0-T05 passes. M0-T07 may consume F3. Any new revision requires a new dispatch;
M0 must not create a merge revision.
