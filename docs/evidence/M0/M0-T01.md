# M0-T01 - Integrated Toolchain Acceptance

## Identity

- Milestone: M0 (trace aliases M01/M02)
- Requirements: NFR-009, NFR-010, NFR-017, CHG-001
- Acceptance criteria: AC-035, AC-040, AC-043, AC-045
- Baseline: `8f6a5e1`; candidate working tree
- Reviewer: Integration/root
- Date: 2026-08-02

## Delivery

The Python, Node, package-manager, framework, test, security, PostgreSQL, and
OpenAPI-generator contracts are exact and reproducible. The canonical record is
`docs/development/toolchain.md`; lane detail and transcripts are in the sibling
backend/frontend compatibility records.

## Validation

| Check | Result |
|---|---|
| Backend universal resolution, frozen/offline sync, exact imports, strict Mypy smoke, tool versions | Pass |
| Backend `pip-audit --local --strict` | Pass; zero known vulnerabilities |
| Frontend strict-peer lock generation and exact frozen install | Pass |
| Frontend TypeScript/lint/format/Vite/Vitest/Playwright/Storybook executable versions | Pass |
| Initial full frontend audit | Fail; three High and one Moderate transitive advisory reproduced |
| Corrective iteration 1: override `sharp` 0.35.3 and `postcss` 8.5.25, regenerate/frozen install | Pass |
| Corrected full frontend audit | Pass; zero vulnerabilities at every severity |
| Dependency license inventories | Pass as inventory; repository-license compatibility remains `R-018` |
| PostgreSQL 17.10 and OpenAPI Generator 7.17.0 multi-platform digest resolution | Pass |
| `git diff --check` | Pass |

Frontend lock SHA-256:
`2885f033b3c657771beaf28664dc0237ce8b2024da6036e2217c41f862ce4123`.
Backend lock SHA-256:
`20a4bc6ee07eeea4641b61d45370687f3ada70604ce3f483de3deb2fed027ac9`.

## Acceptance record

| AC | Status | Evidence |
|---|---|---|
| AC-035 | Pass | Required stack and exact toolchain are frozen; application architecture validation continues in T03/T04. |
| AC-040 | Pass | Every dependency has a documented foundation purpose; no Redis, worker, AI, or speculative runtime was added. |
| AC-043 | Pass for T01 scope | Install and upgrade contracts are documented. Full contributor walkthrough remains T02/M0-T09. |
| AC-045 | Pass | The contract implements accepted ADRs; corrective overrides and rationale are recorded. |

## Decision

M0-T01 passes and F0 is frozen. `R-018` (project license choice) remains an
explicit release blocker but does not block the next foundation tasks.
