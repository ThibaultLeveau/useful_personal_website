# M0-T07 - Environment and Container Acceptance

## Identity

- Milestone: M0 (trace aliases M01/M03)
- Requirements: F2-002, NFR-012, OSS-002
- Acceptance criteria: AC-015, AC-036, AC-041, AC-044
- Reviewer: Integration/root
- Date: 2026-08-02

## Delivery

The infrastructure slice provides aligned environment contracts, digest-pinned
PostgreSQL, private database/media topology, explicit profile-gated migration,
and multi-stage non-root backend/frontend production images. Startup never
migrates, bootstraps, or seeds.

## Validation

| Check                                         | Result                                             |
| --------------------------------------------- | -------------------------------------------------- |
| Compose default/operations profile validation | Pass; migrate appears only with operations profile |
| Backend and frontend production image builds  | Pass                                               |
| Explicit migration from empty PostgreSQL      | Pass; exact current head `20260802_0004`           |
| Backend/frontend health and HTTP smoke        | Pass                                               |
| Runtime UID/GID, read-only, no-new-privileges | Pass; UID/GID 10001                                |
| Image env/history/context secret scan         | Pass                                               |
| Task-owned stack/network/volume cleanup       | Pass; zero remain; unrelated container preserved   |
| Repository policy and Compose config rerun    | Pass                                               |

## Freeze status F5

The environment names, PostgreSQL/local-media topology, non-root images,
health/readiness, explicit migration boundary, administrator bootstrap, and
demo-seed command wiring are frozen. The seed implementation and its production
refusal/idempotency/privacy proof are recorded in
[M0-T07-seed-demo](M0-T07-seed-demo.md).

## Decision

M0-T07 passes locally. The previously named bootstrap and demo-seed dependencies
are implemented and tested; startup still performs neither operation implicitly.
