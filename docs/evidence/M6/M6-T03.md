# M6-T03 - Integrated project lifecycle certification

## Decision

Pass for the M6 project case-study vertical slice on 2026-08-04. An administrator can create and
validate every active field, preview privately, publish now, preserve a frozen live revision while
editing, republish, feature, show/hide, reorder, schedule/reschedule, unpublish, and soft-delete.
Visitors receive only effective allow-listed case-study content, relations, and metadata.

## Gate matrix

| Gate               | Result                                                                                                                                                                 |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Backend            | Ruff format/check and strict Mypy pass across 149 files; authoritative complete run 274/274 at the 85% branch gate; post-correction migration 12/12 and repository 3/3 |
| Migration/database | Empty/prior-head upgrade, downgrade, parity, constraints/triggers/indexes, one head/current `20260802_0007`, seven-SELECT bound, and 10k-project plans pass            |
| Contract           | OpenAPI validation 58 operations/42 paths; 14 project operations; twice-deterministic 133-file client; strict generated compilation pass                               |
| Frontend           | Prettier, ESLint, strict TypeScript, 130/130 Vitest in 29 files, exact-runtime production build, and Storybook build pass                                              |
| Browser/a11y       | Fresh production-stack Playwright 2/2 in 119.2 s; axe zero; no overflow across 320-1920 px light/dark plus 400% equivalent; manual desktop/390 px review pass          |
| Security           | Bandit, pip-audit, production pnpm audit pass; exact-image Trivy reports zero High/Critical and zero secrets                                                           |
| Runtime            | Fresh volumes; explicit roles/migration/permissions/bootstrap; exact images healthy; no demo seed; E2E cleanup leaves zero public projects and retained audit history  |
| Performance        | Collection/detail accessibility 100 and SEO 100; desktop performance 90/98; mobile 66/75 retained as release-wide target variance; CLS 0                               |
| Documentation      | Operator, API, developer, evidence index, performance/query, security, root status, changelog, and traceability updated                                                |

Frontend aggregate coverage remains below a meaningful repository-wide threshold because generated
transport code is included; no aggregate frontend coverage threshold is configured. Project public
and administrator behavior is instead covered by focused unit/component tests plus the complete
real-stack lifecycle.

## Corrective loop

| Finding                                                                 | Classification    | Correction and independent retest                                                                                       |
| ----------------------------------------------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Pending published edits lacked the final republish action               | Product defect    | Added `Publish changes`; editor regression and full browser copy-on-write flow pass                                     |
| Admin narrow toolbar overflow and preview status used an invalid role   | Accessibility/UI  | Reflowed toolbar and used a valid labeled status container; axe and 320 px E2E pass                                     |
| Save on the same edit route left stale local aggregate state            | Product defect    | Apply authoritative mutation response locally; editor and browser republish sequence pass                               |
| Dynamic detail metadata streamed outside the initial head for audits    | SEO compatibility | Disabled metadata streaming; initial-head inspection and repeated Lighthouse SEO 100 pass                               |
| Technology predicate used `lower(value)` against a plain-value index    | Database defect   | Revised sole `0007` expression index; physical-index assertion, 12 migrations, repository suite, and improved plan pass |
| One Lighthouse mobile launch ended before navigation with `NO_NAVSTART` | Tooling variance  | Excluded/overwritten incomplete report; isolated repeat completed with every category                                   |

No waiver, disabled test, mutable publication, generated drift, second migration head, unresolved
WCAG blocker, or confirmed Critical/High finding remains. The mobile performance target is carried
open to M13/M14 and is not represented as passed.

## Privacy and final-state inspection

The fresh database contains two synthetic E2E aggregates, both soft-deleted, zero still-published
aggregates, four frozen retained revisions, and sixteen controlled `project.*` audit events. The
anonymous list returns zero items/total. Public API, SSR HTML, metadata, JSON-LD, screenshots, and
logs contain no draft/future/hidden/deleted content, hidden provider target, creator, pointer,
version, schedule, cookie, CSRF, authorization value, storage key, or fake media value. Failure
traces and generated Playwright reports were removed after the successful run.

## Review and acceptance boundary

Integration/root independently reviewed the project domain, generated contract, migration/index
definition, realistic query plans, exact-image scans, fresh stack, rendered collection/detail at
desktop and 390 px, responsive/theme screenshots, metadata, accessibility, and final database
state after automated lanes completed. There was no external-human reviewer; M14 still requires
the release-wide independent review.

M6 accepts `F1-010` and `F2-006` (trace alias M10) and releases M7 planning/implementation. It does
not authorize media storage/upload, blog, custom pages/blocks, contacts, API tokens, schedulers,
workers, or AI runtime behavior.
