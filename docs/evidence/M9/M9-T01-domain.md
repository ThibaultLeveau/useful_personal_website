# M9-T01-D media domain/application evidence

The media module defines closed lifecycle, format, variant, owner, role, and accessibility catalogs; immutable asset/variant/use records; safe display-name handling; managed-key validation; ready-only selection; checksum-bound upload orchestration; digest-only idempotency; row-locked deletion orchestration; and sanitized append-only audit facts.

Focused domain, service-through-HTTP, configuration, reconciliation, and architecture checks are included in the 45-test S3 gate. Meaningful uses require contextual alt text; decorative uses normalize to an empty alt and reject descriptive alt. No filename-derived alt is implemented.
